"""Helpers shared across the GPUSTACK test suite.

Tests import from this package instead of copying setup code. That keeps
each test short and makes a fix in one helper reach every test at once.
"""
