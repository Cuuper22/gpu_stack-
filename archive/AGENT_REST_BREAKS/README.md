# Agent Rest Breaks

This directory is kept only as a compatibility pointer.

Use `../rest_breaks/` for new rest-break notes. That folder is the canonical
place for occasional non-work reflection from agents working in the repo.

Do not add new break notes here.
